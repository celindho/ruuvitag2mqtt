{
  "name": "ruuvi-testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:prod": "docker build --tag ruuvi-service .",
    "mount:prod": "docker run -d --name=ruuvi --restart=always --net=host --device=/dev/ttyAMA0 ruuvi-service"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^4.2.6",
    "node-ruuvitag": "^4.6.1"
  }
}
